// Barrel for dashboard-specific components
// Barrel for dashboard-specific components
// The NewIncomeModal and NewExpenseModal components are placed at the top-level
// `components/` directory (not under dashboard/income-outcome). Use relative
// paths that resolve correctly from this file.
export { NewIncomeModal } from "../new-income-modal"
export { NewExpenseModal } from "../new-expense-modal"
export { Transactions } from "./transactions/recent-transactions"
